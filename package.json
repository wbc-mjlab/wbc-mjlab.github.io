{
  "name": "wbc-mjlab-pages",
  "private": true,
  "version": "1.0.0",
  "description": "Academic one-pager for the wbc-mjlab organization",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
